Unattended-Upgrade::Origins-Pattern {
// Shipped without a Origins-Pattern, will be generated later by the
// update-Origins-Pattern script which is run by cronjob or when user
// enables unattended upgrade within MX Updater preferences.
};
